the generation succeeded but produced no deliverable outputs — it was charged; inspect it with `civitai workflows get wf_123` (the orchestrator often supplies no failure reason, so it may not say why)
