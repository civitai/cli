About to cancel workflow 01JABCXYZ. ⚠ You are billed for what this run has already delivered; the server re-prices the rest. What the ledger ends up doing is decided server-side — this CLI cannot see your Buzz ledger — `civitai buzz` reports a balance, not a history, so settle it against your Buzz transaction history (/user/transactions). Outputs it has not produced yet are lost. It cannot be un-cancelled. Cancel it? [y/N]:
