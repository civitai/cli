About to cancel workflow 01JABCXYZ. ⚠ This does NOT undo the charge — a mid-run cancel bills the cost already accrued. What the ledger does afterwards is decided server-side — this CLI cannot see your Buzz ledger — `civitai buzz` reports a balance, not a history, so settle it against your Buzz transaction history (/user/transactions). You are throwing away a job you have already paid for. It cannot be un-cancelled. Cancel it? [y/N]:
